# Tasks: Add AI SDK Core Mock Example

## 1. Specification

- [x] 1.1 Add proposal, design, and task list for the AI SDK Core mock slice.
- [x] 1.2 Add a trace writer spec delta for the AI SDK Core example.

## 2. Example

- [x] 2.1 Add the AI SDK dependency through pnpm.
- [x] 2.2 Add a deterministic `generateText` mock-provider example.
- [x] 2.3 Record tool callbacks into Probe JSONL.
- [x] 2.4 Re-parse the generated JSONL in the demo script.

## 3. Documentation

- [x] 3.1 Add a package script for the AI SDK Core demo.
- [x] 3.2 Include the AI SDK Core demo in `pnpm check`.
- [x] 3.3 Document the generated output path and no-key behavior.

## 4. Verification

- [x] 4.1 Run `pnpm demo:ai-sdk-core`.
- [x] 4.2 Run `pnpm check`.
