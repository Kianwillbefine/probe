# Agent Instructions

- Keep changes scoped to the agent probe/replay debugger.
- Prefer small, runnable slices over speculative framework work.
- Use `npm run check` as the default verification command before handoff.
- Do not commit secrets, `.env` files, `node_modules`, or `dist`.

